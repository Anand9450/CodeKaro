{
  "name": "codekaro-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "install:all": "cd apps/backend && npm install && cd ../frontend && npm install",
    "build": "cd apps/frontend && npm run build"
  }
}