SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
# Optional: Use Service Key for Admin Bypass if needed, but Anon Key + RLS is standard.
# For now, we stick to Anon Key as requested.
